
<h1>ASOIAF Family Tree</h1>
<p>On this page, which will also be the home page, the entire family tree is displayed.</p>

<% @people.each do |person| %>
  <p><%= person.name %>
     <%= if @people.find_by(id: person.father) != nil
           @people.find_by(id: person.father).name
         end 
      %></p>
<% end %>
